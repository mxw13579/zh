# Requests must include header `Adapter-Authorization` matching this value.
# Requests must include header `UPSTREAM-BASE-URL` (adapter appends `/v1/chat/completions`).
ADAPTER_AUTHORIZATION=change-me-to-a-long-random-token
HOST_PORT=8787
ALLOW_ORIGIN=*
REASONING_STRATEGY=as_reasoning_content
