{
  "name": "openai-format-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env --import tsx src/server.ts",
    "dev": "node --env-file-if-exists=.env --watch --import tsx src/server.ts",
    "test": "node --import tsx --test"
  },
  "dependencies": {
    "tiktoken": "^1.0.22",
    "tsx": "^4.20.3"
  }
}
